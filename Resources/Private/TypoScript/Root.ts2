prototype(Sandstorm.CrudForms:Template) < prototype(Neos.Fusion:Template) {
    model = ${model}

    // needed for listing
    objects = ${objects}

    // needed for single view
    object = ${object}
}